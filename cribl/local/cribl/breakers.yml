Never Match:
  lib: custom
  tags: never,match
  rules:
    - condition: "true"
      eventBreakerRegex: /$./
      timestampAnchorRegex: /^/
      timestamp:
        type: auto
        length: 150
      timestampTimezone: local
      maxEventBytes: 51200
      name: Never Match
